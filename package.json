{
  "name": "flowy",
  "version": "0.0.1",
  "description": "Yet another Javascript SVG Flowchart Editor",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle --global-name=jssvgflow src/FlowchartEditor.js --outfile=build/jssvgflowedit.js",
    "serve": "esbuild --bundle --global-name=jssvgflow src/FlowchartEditor.js --outfile=example/jssvgflowedit.js --servedir=example"
  },
  "author": "Tom",
  "license": "Apache-2.0",
  "devDependencies": {
    "esbuild": "^0.19.8"
  }
}
